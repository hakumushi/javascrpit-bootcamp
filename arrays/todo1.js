const todos = ['Study english', 'Work', 'Read the emails', 'Grab the lunch', 'Go to the class']

console.log(`You have ${todos.length} todos!`)

console.log(`Todo: ${todos[0]}`)
console.log(`Todo: ${todos[todos.length -2]}`)
